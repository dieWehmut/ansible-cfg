---
- name: Baseline non-NixOS Linux guests
  hosts: debian
  gather_facts: true
  roles:
    - role: common

- name: Docker-capable guests
  hosts: docker
  gather_facts: true
  roles:
    - role: docker
