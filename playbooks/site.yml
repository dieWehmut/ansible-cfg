---
- name: Baseline running Debian LXCs
  hosts: debian_running
  gather_facts: true
  roles:
    - role: common

- name: Docker-capable running LXCs
  hosts: docker_running
  gather_facts: true
  roles:
    - role: docker
